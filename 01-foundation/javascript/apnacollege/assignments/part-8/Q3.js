let strings = ['hello', 'world', 'i', 'am', 'kaushal'];

// Logic: Convert every string to Uppercase using .map()
const newArray = strings.map((i) => i.toUpperCase());

console.log(newArray);  // Output: ["HELLO", "WORLD", "I", "AM", "KAUSHAL"]
