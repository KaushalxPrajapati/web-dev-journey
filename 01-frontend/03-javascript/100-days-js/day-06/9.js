/*
=============================================================================
Problem 9: Variables and Reassignment

Create a variable named `score` with an initial value of 10.

Then:
- Increase the score by 5.
- Decrease the score by 3.
- Multiply the score by 2.

Print the score after each operation.

Use `let` and assignment operators.
=============================================================================
*/

let score = 10;

score += 5;
console.log(score);

score -= 3;
console.log(score);

score *= 2;
console.log(score);
