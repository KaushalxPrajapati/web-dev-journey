/*
=============================================================================
Problem 8: Basic Expressions

Create two number variables.

Calculate and print the result of:
- Adding the two numbers
- Multiplying the two numbers
- Calculating the average of the two numbers

Store each calculated result in a separate variable before printing it.
=============================================================================
*/

const a = 2;
const b = 3;

const sum = a + b;
const product = a * b;
const average = (a + b) / 2;

console.log(`Adding: ${sum}`);
console.log(`Multiplying: ${product}`);
console.log(`Calculating Average: ${average}`);
